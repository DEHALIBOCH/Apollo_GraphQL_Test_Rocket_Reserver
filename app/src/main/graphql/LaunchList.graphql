query LaunchList {
    launches {
        launches {
            id
            site
            mission {
                name
                missionPatch(size: SMALL)
            }
        }
    }
}